To start front we need vite and npm libs.
might need to update npm and maybe apt update / upgade
then we go to the proper folder in this case gfront/front
and we run npm run dev (for dev env)

To start back we go to server and use the makefile to build or run the db
we do make to get the exec and then we run the backend app.
